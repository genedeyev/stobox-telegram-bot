# Portable process definitions (Render / Heroku / Fly / Railway Nixpacks).
# The Telegram bot is a long-lived WORKER (polling), not a web server.
worker: python -m stobox_ai
# Optional web service: chat-widget API + HMAC /api/reingest self-update webhook.
web: stobox-web
